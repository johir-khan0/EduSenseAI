# Example env file - copy this to `.env.local` and fill in the real values.
# Do NOT commit your real `.env.local` to Git.

# Gemini (Google) API key - required if you use the Gemini provider.
GEMINI_API_KEY=your_gemini_api_key_here

# Optional / future provider keys
# OPENAI_API_KEY=your_openai_api_key_here
# CLAUDE_API_KEY=your_claude_api_key_here

# If you add server-side secrets, keep them in your server environment or
# GitHub Secrets and never commit them to the repo.
